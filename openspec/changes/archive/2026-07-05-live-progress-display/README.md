# live-progress-display

Replace default SLF4J console output with a live TUI showing task progress, per-worker interaction timelines, loop progress, timing, and token usage. Keep --execution-trace for verbose mode. Log files always written.
