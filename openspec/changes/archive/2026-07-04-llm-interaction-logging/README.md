# llm-interaction-logging

Replace ChatListener with direct logging in ChatExecutor. Console shows progressive per-round messages (no redundancy). File gets complete conversation with thinking/token usage. Thread-safe by instance isolation. Prepare for streaming.
